# Release 0.4.0

Date: 2026-07-04
Summary: async query-ID-only execution and release hardening

## Changes

- Requires repository `query_path` or direct `query_id` for all runtime NQE execution.
- Uses Forward 26.6+ async query-ID execution for full snapshot fetches.
- Removes raw inline NQE execution fallback.
- Removes diff-to-full-query fallback; diff failures now fail with slice attribution.
- Carries release hardening: encrypted profile secrets, security policy, validation matrix,
  zero-skip non-integration gate, and SSoT class-path regression coverage.

## Verification

- `python scripts/ci_local.py`
- Live 26.6 smoke not run from this shell because `FORWARD_LIVE_*` credentials were
  not present in the environment; run before production rollout in a credentialed shell.

## Rollout

- `python scripts/release.py 0.4.0 --summary "async query-ID-only execution and release hardening" --publish`
